#include<cstdio>
#include<algorithm>
using namespace std;
int main(){
	pair<int,int> a[10];
	for(int i=0;i<10;i++)
		a[i].first=5+i%2,a[i].second=i;
	sort(a,a+10);
	for(int i=0;i<10;i++)printf("%d %d\n",a[i].second,a[i].first);
}
